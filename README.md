# angular-2-asterisk-and-template

Shows how `ngIf`, `ngSwitch` and `ngFor` add and remove element subtrees that are wrapped in `<template>` tags.

Used by tutorials and examples at [lishman.io](http://lishman.io)
